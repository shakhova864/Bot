def equation(m, c):
    e = float(m) * (float(c) ** 2)
    return e

def gravitation(F, r, m1, m2):
    g = (int(F) * int(r)**2) / (int(m1) * int(m2))
    return g

def planka(E, v):
    h = int(E) / int(v)
    return h